% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{sig_collection_stats}
\alias{sig_collection_stats}
\title{Compute Summary Statistics for a Signature Collection}
\usage{
sig_collection_stats(signatures)
}
\arguments{
\item{signatures}{A \code{sigverse} signature collection (named list of signature data.frames).}
}
\value{
A \code{data.frame} with one row per signature and columns for each computed metric.
}
\description{
Calculates a panel of descriptive statistics for each signature in a \code{sigverse} collection.
These metrics quantify different aspects of a mutational signature's shape — such as inequality,
diversity, concentration, and sparsity - and are useful for comparing how "flat", "focal", or
"distinctive" different signatures are.
}
\details{
For each signature, the following metrics are reported:
\itemize{
\item \strong{\code{gini}}: Measures inequality (0 = perfectly flat; 1 = all weight in one context).
\item \strong{\code{shannon_index}}: Entropy of the distribution (higher = more uncertain/diverse).
\item \strong{\code{shannon_index_exp}}: Effective number of active contexts (e.g., 96 = flat, 1 = peaked).
\item \strong{\code{shannon_index_exp_scaled}}: Fraction of maximum possible diversity (0–1 scale).
\item \strong{\code{kl_divergence_from_uniform}}: Divergence from a uniform (flat) distribution.
\item \strong{\code{l1_norm}}: Total absolute weight (larger = more mass in fewer contexts).
\item \strong{\code{l2_norm}}: Magnitude of the vector; emphasizes focal peaks.
\item \strong{\code{l3_norm}}: Amplifies concentration even more than L2.
\item \strong{\code{l0_norm}}: Number of non-zero contexts (i.e., how many are active at all).
\item \strong{\verb{*_scaled} variants}: Norms divided by number of contexts to allow cross-signature comparisons.
\item \strong{\code{max_channel_fraction}}: Highest single-context weight (equivalent to the infinity norm).
}

This function is optimised for speed and returns a data.frame with one row per signature
and columns for each computed metric.
}
\examples{
library(sigstash)
signatures <- sig_load("COSMIC_v3.3.1_SBS_GRCh38")

# Compute statistics for all signatures
stats <- sig_collection_stats(signatures)
head(stats)

# Examine metrics for a single signature
stats[stats$id == "SBS1", ]

}
\seealso{
\code{\link[=sig_shannon]{sig_shannon()}}, \code{\link[=sig_gini]{sig_gini()}}, \code{\link[=sig_kl_divergence]{sig_kl_divergence()}}, \code{\link[=sig_l2_norm]{sig_l2_norm()}}
}
