% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{sig_l2_norm}
\alias{sig_l2_norm}
\title{Compute the L2 Norm of a Signature or Catalogue}
\usage{
sig_l2_norm(signature, value = c("fraction", "count"), scale = FALSE)
}
\arguments{
\item{signature}{A \code{sigverse} signature or catalogue data.frame.}

\item{value}{Character string, either \code{"fraction"} or \code{"count"}, indicating which column to compute the norm on.}

\item{scale}{Logical. If \code{TRUE}, divides the norm by the number of elements to enable easier comparison across different signature sizes.}
}
\value{
A single numeric value representing the L2 norm.
}
\description{
Calculates the \strong{L2 norm} (Euclidean norm) of a \code{sigverse} signature or catalogue.
This provides a quantitative measure of how concentrated or dispersed the distribution is.
}
\details{
For a vector \( x \), the L2 norm is defined as:
\deqn{\|x\|_2 = \sqrt{\sum_i x_i^2}}

Interpretation:
\itemize{
\item A signature with a \strong{uniform distribution} has a \strong{lower} L2 norm.
\item A \strong{peaked signature} (one dominant context) has a \strong{higher} L2 norm.
}

This is an alternative to entropy-based metrics (like Shannon index), and useful
for quickly identifying signatures with strong focal points.
}
\examples{
library(sigstash)

signatures <- sig_load("COSMIC_v3.3.1_SBS_GRCh38")

# Compute L2 norm on fractional signature
sig_l2_norm(signatures[["SBS1"]])

# Compare with a flatter signature
sig_l2_norm(signatures[["SBS3"]])

# Compute on raw counts (requires a catalogue)
cat1 <- sig_reconstruct(signatures[["SBS3"]], n = 100)
sig_l2_norm(cat1, value = "count")

}
\seealso{
\code{\link[=sig_shannon]{sig_shannon()}}, \code{\link[=sig_kl_divergence]{sig_kl_divergence()}}, \code{\link[=sig_gini]{sig_gini()}}
}
