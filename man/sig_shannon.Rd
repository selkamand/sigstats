% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{sig_shannon}
\alias{sig_shannon}
\title{Calculate the Shannon Diversity Index for a Signature}
\usage{
sig_shannon(signature, exponentiate = FALSE)
}
\arguments{
\item{signature}{A \code{sigverse} signature data.frame. See \code{\link[sigshared:signature]{sigshared::example_signature()}}.}

\item{exponentiate}{Logical. If \code{TRUE}, returns the exponentiated Shannon index (effective number of contexts).}
}
\value{
A numeric value: either the Shannon index (entropy) or the exponentiated index (effective diversity).
}
\description{
Computes the Shannon diversity index for a \code{sigverse} signature. This metric
quantifies the entropy or uncertainty associated with the distribution of mutation
contexts in a signature, based on the relative \code{fraction} of mutations in each context.
}
\details{
By default, the function returns the Shannon index as an entropy value. If \code{exponentiate = TRUE},
the function returns the \strong{exponentiated Shannon index}, also known as the \strong{effective number
of contexts} (or Hill number of order 1). This makes interpretation more intuitive:
\itemize{
\item A signature concentrated entirely in a single context has an exponentiated index of 1.
\item A perfectly uniform signature (equal weight across all 96 SBS contexts) has an exponentiated index of 96.
}

In biological terms, the exponentiated Shannon index answers:
\emph{"How many equally frequent mutation contexts would give this level of diversity?"}
}
\examples{
library(sigstash)
signatures <- sig_load("COSMIC_v3.3.1_SBS_GRCh38")
sbs3 <- signatures[["SBS3"]]
sbs48 <- signatures[["SBS48"]]

# Shannon entropy
sig_shannon(sbs3)

# Exponentiated Shannon index (effective # of active contexts)
sig_shannon(sbs3, exponentiate = TRUE)

# Compare with a highly focused signature
sig_shannon(sbs48, exponentiate = TRUE)

}
