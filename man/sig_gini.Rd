% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sig_maths.R
\name{sig_gini}
\alias{sig_gini}
\title{Compute the Gini Coefficient of a Signature or Catalogue}
\usage{
sig_gini(signature)
}
\arguments{
\item{signature}{A \code{sigverse} signature or catalogue data.frame.}
}
\value{
A numeric value between 0 and 1 representing the Gini coefficient.
}
\description{
Calculates the \strong{Gini coefficient}, a measure of inequality or concentration,
for a \code{sigverse} signature or catalogue. It ranges from:
}
\details{
\itemize{
\item \strong{0}: perfectly uniform distribution (e.g. all 96 contexts equal)
\item \strong{1}: total concentration in a single context
}

The Gini coefficient complements entropy-based measures like Shannon index by capturing
the \emph{inequality} of the distribution, rather than uncertainty or diversity.

This function uses the \strong{biased version} of the Gini coefficient (dividing by \emph{n}), which is:
\itemize{
\item The \strong{standard definition} used in descriptive analysis
\item More appropriate when the signature is a full population profile rather than a random sample
\item Consistent with usage in mutation signature literature
}
}
\examples{
library(sigstash)
sigs <- sig_load("COSMIC_v3.3.1_SBS_GRCh38")

sig_gini(sigs[["SBS1"]])  # moderately peaked
sig_gini(sigs[["SBS48"]]) # highly peaked, close to 1


}
\seealso{
\code{\link[=sig_shannon]{sig_shannon()}}, \code{\link[=sig_kl_divergence]{sig_kl_divergence()}}, \code{\link[=sig_l2_norm]{sig_l2_norm()}}
}
