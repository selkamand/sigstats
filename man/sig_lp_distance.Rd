% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sig_maths.R
\name{sig_lp_distance}
\alias{sig_lp_distance}
\title{Compute L\if{html}{\out{<sub>}}p\if{html}{\out{</sub>}} Distance Between Two Signatures}
\usage{
sig_lp_distance(
  signature1,
  signature2,
  p,
  value = c("fraction", "count"),
  scale = FALSE
)
}
\arguments{
\item{signature1, }{signature2 Two \code{sigverse} signature or catalogue data.frames.}

\item{p}{A numeric value ≥ 1 indicating the order of the L\if{html}{\out{<sub>}}p\if{html}{\out{</sub>}} norm to compute.}

\item{value}{Either \code{"fraction"} (default) or \code{"count"} — determines which column is used for comparison.}

\item{scale}{Logical. If \code{TRUE}, distance is divided by the number of contexts (i.e. channels).}
}
\value{
A non-negative numeric value representing the L\if{html}{\out{<sub>}}p\if{html}{\out{</sub>}} distance between the two profiles.
}
\description{
Calculates the L\if{html}{\out{<sub>}}p\if{html}{\out{</sub>}} distance (also known as the Minkowski distance)
between two \code{sigverse} signatures or catalogues. This generalizes various distance
metrics depending on the choice of \code{p}:
}
\details{
\itemize{
\item \code{p = 1}: Manhattan distance (sum of absolute differences)
\item \code{p = 2}: Euclidean (L2) distance
\item \verb{p = ∞}: Chebyshev distance (maximum absolute difference)
}

This function is useful for flexible distance computations when comparing
mutational signatures or catalogues. All channels must match and be in the same order.

By default, distances are computed using raw values. If \code{scale = TRUE}, the distance is
divided by the number of mutation contexts to allow comparisons across different signature types
(e.g. SBS vs DBS).
}
\examples{
library(sigstash)
sigs <- sig_load("COSMIC_v3.3.1_SBS_GRCh38")

sig_lp_distance(sigs[["SBS1"]], sigs[["SBS5"]], p = 1)  # L1 (Manhattan)
sig_lp_distance(sigs[["SBS1"]], sigs[["SBS5"]], p = 2)  # L2 (Euclidean)
sig_lp_distance(sigs[["SBS1"]], sigs[["SBS5"]], p = 1, scale = TRUE)

}
\seealso{
\code{\link[=sig_l2_distance]{sig_l2_distance()}}, \code{\link[=sig_cosine_similarity]{sig_cosine_similarity()}}
}
